# installation
``` bash
hugo add newsite mysite
cd mysite
git init
git submodule add htts:<theme url> thems/<theme>
```

# ox-hugo
## post
C-c c b

## generate md files
C-c C-e H A

# server (and generation)
    hugo server (-D)

