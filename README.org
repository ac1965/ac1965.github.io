#+TITLE: No Good Father
#+AUTHOR: YAMASHITA, Takao
#+DESCRIPTION: Hugo + Blowfish テーマで構築された個人ブログ
#+OPTIONS: toc:t num:nil ^:nil

#+begin_quote
[[https://gohugo.io/][Hugo]] + [[https://blowfish.page/][Blowfish]] テーマで構築された個人ブログ

Emacs の [[https://orgmode.org/][Org-mode]] と [[https://ox-hugo.scripter.co/][ox-hugo]] により記事を作成
#+end_quote

* 目次                                                              :TOC_2:
- [[#requirements][Requirements]]
- [[#installation][Installation]]
- [[#submodule-管理][Submodule 管理]]
- [[#writing-workflow][Writing Workflow]]
- [[#local-development][Local Development]]
- [[#configuration][Configuration]]
- [[#blowfish-テーマについて][Blowfish テーマについて]]
- [[#deploy][Deploy]]
- [[#known-issues][Known Issues / 運用上の注意]]

* Requirements
:PROPERTIES:
:CUSTOM_ID: requirements
:END:

| ツール  | バージョン        | 備考                    |
|---------+------------------+-------------------------|
| [[https://gohugo.io/installation/][Hugo]]    | 0.162.0〜0.165.0 extended（テーマの ~min~/~max~、[[#blowfish-テーマについて][Blowfish テーマについて]] 参照） | ~brew install hugo~ |
| [[https://git-scm.com/][Git]]     | 任意             | submodule 管理用        |
| [[https://www.gnu.org/software/emacs/][Emacs]]   | ≥ 29             | 記事執筆用              |
| [[https://ox-hugo.scripter.co/][ox-hugo]] | 最新             | Org → Hugo Markdown     |
| [[https://github.com/mermaid-js/mermaid-cli][mmdc]]    | 任意             | Mermaid 図の生成        |

* Installation
:PROPERTIES:
:CUSTOM_ID: installation
:END:

** 1. Hugo サイトの作成

#+begin_src bash
hugo new site mysite
cd mysite
git init
#+end_src

*重要*: サイトのルートディレクトリは *iCloud Drive
（「デスクトップと書類フォルダ」の同期対象、すなわち ~$HOME/Documents~ や
~$HOME/Desktop~ 配下）に置かないこと。過去にこれが原因で同期競合コピー
（例: ~cover 2.jpg~）が自動生成され、~deploy.sh~ の既存ファイル判定を
すり抜けて画像が壊れる事故が発生している（詳細は
[[#known-issues][Known Issues]] を参照）。~$HOME/Projects/<repo>/mysite~
のような、iCloud同期対象外の場所に置くこと。

** 2. Blowfish テーマを submodule として追加

#+begin_src bash
git submodule add -b main https://github.com/nunocoracao/blowfish.git themes/blowfish
#+end_src

** 3. テーマ設定ファイルのコピー

#+begin_src bash
cp themes/blowfish/config/_default/* config/_default/
#+end_src

このコピーは初回セットアップ時のみです。テーマ更新時の設定反映は
[[#submodule-管理][Submodule 管理]] を参照してください。

** 4. Emacs パッケージのインストール

~straight.el~ を利用（~orgx/orgx-export.el~ で設定済み）

#+begin_src emacs-lisp
M-x straight-pull-all
#+end_src

ox-hugo がロードされていることを確認

#+begin_src emacs-lisp
M-: (featurep 'ox-hugo)  ; => t
#+end_src

* Submodule 管理
:PROPERTIES:
:CUSTOM_ID: submodule-管理
:END:

（変更なし。以下省略せず既存のまま維持）

Blowfish テーマは Git submodule として管理されています（~.gitmodules~ で
~branch = main~ を追跡。Hugo Modules（~go.mod~）は使用していません）。
リポジトリを clone した直後や、テーマ更新時は以下を実行します。

** 初回 clone 時（submodule を含める）

#+begin_src bash
git clone --recursive https://github.com/<your-repo>.git
cd <your-repo>
#+end_src

すでに clone 済みの場合：

#+begin_src bash
git submodule update --init --recursive
#+end_src

** Blowfish テーマを最新に更新する手順

Blowfish は SemVer タグ（~vX.Y.Z~）を ~main~ ブランチ上のコミットに直接
打つ運用（別リリースブランチは存在しない）です。本リポジトリの
~.gitmodules~ はタグではなく ~branch = main~ を追跡しているため、
実質的に「最新タグ」を追随することになりますが、タグ固定はしていない
点に注意し、更新時は「取得して終わり」ではなく以下の順番で確認します。

*** 1. 現在のバージョンを記録（切り戻し用）

#+begin_src bash
git -C themes/blowfish rev-parse HEAD > /tmp/blowfish-before.rev
#+end_src

*** 2. submodule を最新に更新

#+begin_src bash
git submodule update --remote --merge themes/blowfish
#+end_src

*** 3. 設定ファイルの差分を確認

テーマ側 ~config/_default/~ に新しいキーが増えていないか必ず diff します。
差分があれば自サイトの ~config/_default/~ に手動でマージしてください
（上書きコピーはしないこと。ローカルのカスタマイズが失われます）。

#+begin_src bash
diff -ru config/_default/ themes/blowfish/config/_default/
#+end_src

*** 4. Hugo バージョン要件を確認

Hugo の min/max バージョン要件は ~themes/blowfish/config.toml~ の
~[module.hugoVersion]~ で管理されています
（~themes/blowfish/config/_default/module.toml~ は v3 以降空ファイルになり
使われていません）。~min~ が上がっていないか、ローカルの Hugo が ~max~ を
超えていないかの両方を確認し、必要ならローカルの Hugo と CI 側を追随させます。

#+begin_src bash
cat themes/blowfish/config.toml
hugo version
#+end_src

CI（~.github/workflows/pages.yml~）の ~HUGO_VERSION~ もこの値に合わせて
更新すること（[[#deploy][Deploy]] 参照）。ここがズレると、ローカルでは
ビルドできるのに CI だけ失敗する事態になります。

*** 5. ローカルビルドで検証してからコミット

#+begin_src bash
hugo server --buildDrafts --disableFastRender
#+end_src

- ~failed to extract shortcode~ 等のエラーが出ないこと
- WARN の deprecated 設定キーが増えていないこと（出ていれば
  [[#configuration][Configuration]] のファイルを合わせて修正）
- ~Resize~ 関連のエラー（~image: unknown format~ 等）が出ないこと。
  拡張子と実データの形式が一致しない画像が紛れ込んでいる可能性がある
  （[[#known-issues][Known Issues]] 参照）

問題なければコミットします：

#+begin_src bash
git add themes/blowfish config/_default
git commit -m "chore: update blowfish theme"
#+end_src

問題があれば、記録しておいた commit に戻します：

#+begin_src bash
cd themes/blowfish && git checkout $(cat /tmp/blowfish-before.rev) && cd ../..
git add themes/blowfish
git commit -m "revert: rollback blowfish theme update"
#+end_src

** すべての submodule を更新

#+begin_src bash
git submodule update --remote --recursive
#+end_src

** Submodule 状態確認

#+begin_src bash
git submodule status
#+end_src

** Submodule を強制的に同期

リモートURL変更などがあった場合：

#+begin_src bash
git submodule sync --recursive
git submodule update --init --recursive
#+end_src

** よくあるトラブル対処

submodule が壊れた場合：

#+begin_src bash
git submodule deinit -f themes/blowfish
rm -rf .git/modules/themes/blowfish
git submodule update --init --recursive
#+end_src

* Writing Workflow
:PROPERTIES:
:CUSTOM_ID: writing-workflow
:END:

すべての記事は *Org-mode* で作成し、ox-hugo により Markdown へ変換されます。

** ディレクトリ構成（現状）

#+begin_export hugo
{{< alert >}}
以前このセクションでは ~content/post/<slug>.md~ という flat な
単一ファイルで出力される旨を記載していたが、2026-07-30 の
全記事一括移行（コミット ~c71d7f32~）により、現在は
~content/post/<slug>/index.md~ という真の Page Bundle 形式に
統一されている。それに伴い、当時「既知の設計負債」としていた
flat file 由来の制約（同名ディレクトリがPage Resourceとして
認識されない等）は解消済み。記述を実態に合わせて訂正した。
{{< /alert >}}
#+end_export

#+begin_example
all-posts.org（記事のOrgソース。1ファイルに複数記事をsubtreeで格納）

content/ (ox-hugo または org2hugo.py により生成。2026-09-21以降 Git 管理下)
├── about/
│   └── index.md              ← Page Bundle
└── post/
    └── <slug>/
        ├── index.md          ← 記事本体（Page Bundle）
        ├── cover.jpg         ← deploy.sh/prepare-content.sh が自動配置
        └── (記事内の画像等)   ← Page Resource として正しく認識される
#+end_example

すべての記事が Page Bundle（~<slug>/index.md~）形式なので、記事本文や
front matter から参照する画像はディレクトリ内に置くだけで Blowfish の
Page Resource 機構（~.Resources.ByType "image"~ 等）から認識される。
カバー画像だけは記事ごとの手動指定・自動選定の余地を残すため、
引き続き ~prepare-content.sh~（[[#deploy][Deploy]] 参照）が機械的に配置する
運用にしている。

** Org ファイル Front Matter 例

#+begin_src org
,* 記事タイトル
:PROPERTIES:
:EXPORT_FILE_NAME: my-post
:EXPORT_DATE: 2026-03-15
:EXPORT_HUGO_TAGS: hugo emacs
:EXPORT_HUGO_CATEGORIES: tech
:END:
#+end_src

#+begin_export hugo
{{< alert >}}
front matter の ~date~／~publishDate~ は、単なる表示用メタデータでは
なく ~deploy.sh~ のカバー画像ローテーション判定にも使われる
（[[#deploy][Deploy]] 参照）。~EXPORT_DATE~ を空にしたまま公開すると、
そのファイルはカバー画像配置の対象からスキップされる。
{{< /alert >}}
#+end_export

** Export コマンド

| 操作               | キーバインド    | 説明                        |
|--------------------+-----------------+-----------------------------|
| 新規投稿作成       | ~C-c c b~ 相当 | org-capture で投稿作成（テンプレート ~b~） |
| サブツリー export  | ~C-c C-e H H~   | 現在 subtree を export      |
| 全体 export        | ~C-c C-e H A~   | Org ファイル全体 export     |

保存時（または capture 確定時）に ~org-hugo-export-wim-to-md~ が
自動実行される設定になっている場合、手動 export は不要。

* Local Development
:PROPERTIES:
:CUSTOM_ID: local-development
:END:

** 開発サーバ起動（draft 含む）

#+begin_src bash
hugo server --buildDrafts --disableFastRender
#+end_src

Open: [[http://localhost:1313]]

テーマ（Blowfish）の更新手順は [[#submodule-管理][Submodule 管理]] に統一しています。

** よく使う Hugo コマンド

#+begin_src bash
# 本番ビルド
hugo

# リンクチェック
hugo --gc --minify

# バージョン確認
hugo version
#+end_src

* Configuration
:PROPERTIES:
:CUSTOM_ID: configuration
:END:

設定ファイルは ~config/_default/~ に配置

| ファイル              | 用途                              |
|-----------------------+-----------------------------------|
| ~hugo.toml~           | サイト全体設定                    |
| ~params.toml~         | Blowfish テーマ設定               |
| ~languages.en.toml~   | 言語・著者情報                    |
| ~menus.en.toml~       | ナビゲーション                    |
| ~markup.toml~         | Markdown / syntax highlight       |

* Blowfish テーマについて
:PROPERTIES:
:CUSTOM_ID: blowfish-テーマについて
:END:

[[https://blowfish.page/][Blowfish]] は [[https://nunocoracao.github.io/][Nuno Coração]] が開発する
Hugo 向けの高機能テーマ。本サイトでは ~themes/blowfish~ に Git submodule として
組み込んでいます（導入・更新手順は [[#submodule-管理][Submodule 管理]] を参照）。

** リンク

| 用途           | URL                                                           |
|----------------+----------------------------------------------------------------|
| 公式サイト     | [[https://blowfish.page/]]                                    |
| ドキュメント   | [[https://blowfish.page/docs/]]                                |
| GitHub リポジトリ | [[https://github.com/nunocoracao/blowfish]]                 |
| Changelog      | [[https://github.com/nunocoracao/blowfish/releases]]           |

** バージョン運用上の注意

（変更なし、既存のまま）

Blowfish は SemVer タグ（~vX.Y.Z~）を ~main~ ブランチ上のコミットに直接
打つ運用です。本リポジトリの ~.gitmodules~ ではタグではなく
~branch = main~ を追跡しているため、実質的に最新タグを追随しますが、
タグへの pin はしていません。

- 「最新版」を取り込む＝~main~ の最新コミットを取り込むことを意味し、
  タグ固定によるロールバック起点が無い点に注意（切り戻し手順は
  [[#submodule-管理][Submodule 管理]] の更新手順内に記載）
- Hugo の min/max バージョンは ~themes/blowfish/config.toml~ の
  ~[module.hugoVersion]~ で管理される（旧 ~config/_default/module.toml~ は
  v3 以降空ファイルで未使用）。更新の都度、ローカルとCI（[[#deploy][Deploy]] の
  ~hugo-version~）の両方を ~min~〜~max~ の範囲に収める必要がある

** 本サイトで使用している主な機能

| 機能                      | 設定/実体                                        |
|---------------------------+---------------------------------------------------|
| カラースキーム・レイアウト | ~config/_default/params.toml~                     |
| 記事カバー画像            | 各記事ディレクトリの ~cover.*~（~deploy.sh~ が自動配置。真の Page Bundle ではないため Blowfish 標準の自動検出は非対応） |
| ~carousel~ 等の shortcode | ~themes/blowfish/layouts/shortcodes/~ 内蔵ショートコード |
| 多言語設定の土台          | ~config/_default/languages.en.toml~（英語のみ運用） |

新しい Blowfish の機能（追加ショートコード・新規 params 項目など）を使う場合は、
テーマ更新後に ~themes/blowfish/exampleSite/~ 配下のサンプル設定を参照すると
該当する ~params.toml~ のキーを確認しやすい。

* Deploy
:PROPERTIES:
:CUSTOM_ID: deploy
:END:

#+begin_export hugo
{{< alert >}}
2026-09-21、公開の仕組みをローカル手動デプロイ（~deploy.sh~ が
ビルドから ~deploy~ ブランチへの push まで一括で行う方式）から、
GitHub Actions によるビルド・公開へ移行する作業に着手した。
以下は新方式の設計・実装を反映しているが、*まだ本番切り替えは
完了していない*: ~content/post/~ の初回コミット・push、および
リポジトリの Settings > Pages > Source を「GitHub Actions」へ
切り替える作業がこれから必要。それまでは現行の ~deploy~ ブランチ
（legacy branch deploy）が実際の公開経路のまま。
{{< /alert >}}
#+end_export

** 新方式の全体像

1. Org-mode（~all-posts.org~）で執筆し、見出しを ~DONE~ にする
2. ox-hugo（または ~org2hugo.py~）で ~content/post/~ を生成
3. ~prepare-content.sh~ で画像同期・カバー画像の自動配置
4. ~content/~ を ~git add~ → commit → push（*手動*。~content/~ は
   もはやビルド成果物ではなく Git 管理下のファイル）
5. push をトリガーに GitHub Actions（~.github/workflows/pages.yml~）が
   起動し、Hugo でビルドして GitHub Pages へ直接デプロイする

旧方式にあった ~PROJECTS_ROOT~・~PUBLIC_DIR~（~deploy/~ という別
ワークツリー）・~deploy~ ブランチへの手動 push という概念は、本番切り替え
完了後は不要になる。

** GitHub Actions（~.github/workflows/pages.yml~）

#+begin_src yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

env:
  HUGO_VERSION: "0.165.0"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: ${{ env.HUGO_VERSION }}
          extended: true
      - run: hugo --minify
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
#+end_src

~HUGO_VERSION~ はテーマ更新時に変わる ~themes/blowfish/config.toml~ の
~[module.hugoVersion]~ の ~min~〜~max~ 要件に必ず追随させること
（[[#submodule-管理][Submodule 管理]] 参照）。放置すると
ローカルビルドは通るのに CI だけ失敗する原因になります。

このワークフローが実際に機能するには、リポジトリの
Settings > Pages > Build and deployment > Source を
「GitHub Actions」に切り替える必要がある（現状は「Deploy from a
branch（~deploy~）」のまま）。

** 手動ビルド（ローカルでの確認用）

#+begin_src bash
hugo --minify
# 出力先: ./public/（Git管理対象外のまま）
#+end_src

** prepare-content.sh / deploy.sh

#+begin_export hugo
{{< alert >}}
旧 ~deploy.zsh~ → ~deploy.sh~（ビルド・push込みの一括スクリプト）から、
2026-09-21 に ~prepare-content.sh~（画像同期・カバー配置のみ）を
分離した。~deploy.sh~ 自体は ~prepare-content.sh~ を呼ぶだけの薄い
ラッパーに縮小し、Hugo ビルド・~deploy~ ディレクトリへの同期・
commit・push は行わなくなった（廃止した ~PROJECTS_ROOT~ /
~PUBLIC_DIR~ / ~--dry-run~ も参照）。
{{< /alert >}}
#+end_export

~content/post/~ が既に生成済みであることを前提に、画像同期とカバー画像の
自動配置だけを行う。

*** 使い方

#+begin_src bash
cd ~/Projects/<repo>/mysite
./deploy.sh              # 実体は ./prepare-content.sh を呼ぶだけ
# または直接
./prepare-content.sh
#+end_src

処理が終わったら、次を手動で行う（自動 push はしない。レビューを挟むため
意図的に手動にしている）。

#+begin_src bash
git add content
git commit -m "content: ..."
git push
#+end_src

push すると GitHub Actions が起動し、ビルドと公開が走る
（Pages の Source 切り替え後に限る）。

*** オプション

| オプション       | 説明                                                |
|------------------+-----------------------------------------------------|
| ~--skip-cover~   | カバー画像の自動配置をスキップする                   |
| ~--force~        | 同期競合ファイル（後述）を検出しても処理を続行する    |
| ~--help~ / ~-h~  | 使い方を表示                                         |

旧版にあった ~--dry-run~ は廃止した。ビルドと push がスクリプトから
無くなり、スキップする対象が無くなったため。

*** 処理の流れ

1. *Pre-flight checks* — ~rsync~ の存在と ~content/post/~ の存在を確認。
   加えて、~content/post/~ 配下に ~"foo 2.jpg"~ のような同期競合コピー
   らしき命名のファイルがないか走査し、見つかれば ~--force~ 指定がない限り
   処理を中断する
2. *Syncing post-local assets* — ~assets/img/post-assets/<slug>/~ の
   実ファイルを ~content/post/<slug>/~ へ ~rsync~ で同期
3. *Placing cover images* — カバー未設定の記事に、front matter の
   ~date~／~publishDate~ から読み取った公開月ベースでカバー画像を配置。
   - 冪等: 既存の ~cover.*~ に加え、~cover 2.jpg~ のようなスペース・
     ハイフン区切りの亜種も「既存扱い」として上書きしない
   - front matter の日付が解析できない記事は、実行時点の月への
     フォールバックを *行わず* スキップし、最後に警告として一覧表示する
     （季節画像が記事ごとの公開日と無関係に同一化するバグの再発防止）

途中でエラーが出た場合、実行は各ステップの ~abort~ で即座に中断されます
（~set -euo pipefail~）。~carousel~ shortcode 未検出のようなビルドエラーは
[[#submodule-管理][Submodule 管理]] の「Blowfish テーマを最新に更新する手順」で
案内している設定差分確認が漏れているケースが多いので、あわせて確認してください。

* Known Issues / 運用上の注意
:PROPERTIES:
:CUSTOM_ID: known-issues
:END:

** iCloud 同期競合によるカバー画像破損（2026-03 発生・対策済み）

サイトを ~$HOME/Documents~ 配下に置いていた時期、iCloud の同期デーモンが
編集中のファイルを競合と誤判定し、~"cover 2.jpg"~ のような名前のファイルを
自動生成する事象が発生した。これが ~deploy.sh~（当時）のカバー画像存在
チェック（~cover.*~ のみに一致するグロブ）をすり抜け、正規の ~cover.jpg~
と重複配置され、結果としてカバー画像が季節画像で無差別に上書きされる
不具合を引き起こしていた。

さらに調査の過程で、2026-03-21 の単一コミットで *46記事のうち
ほぼ全部の本文・カバー画像・記事内画像が誤って一括削除されていた*
ことが判明した。この削除は3ヶ月半以上気づかれないまま放置されていた。
Git 履歴の直前コミットから該当ファイルを ~git show <commit>:<path>~
で復元し、事なきを得ている。

*対策として実施済み*:
- サイトルートを iCloud 同期対象外の ~$HOME/Projects~ 配下へ移動
- ~deploy.sh~ の既存カバー判定を拡張し、亜種ファイル名も検出
- ~deploy.sh~ に同期競合ファイルの事前検査（pre-flight check）を追加
- front matter 日付解析失敗時のフォールバック挙動を「実行時月で代用」
  から「スキップ＋警告」へ変更

** ~resources/_gen/~・~public/~ を誤って Git 管理下に置かない

いずれも Hugo のビルド成果物・キャッシュであり、~hugo~ 実行のたびに
自動再生成される。過去に誤ってコミットされていたことがあるため、
~.gitignore~ に必ず含めること。

#+begin_src gitignore
resources/_gen/
public/
#+end_src

** 画像ファイルの拡張子と実データ形式の不一致に注意

画像を手動で配置・復元する際、拡張子だけを ~.jpg~ に変更して中身が
PNG のまま、といった不一致があると、Hugo の ~.Resize~ 処理が
~image: unknown format~ でビルド全体を失敗させる。復元・移動の際は
必ず ~file <path>~ で実データの形式を確認し、拡張子と一致させること。

** 記事内画像（本文からレファレンスされる画像）について

2026-07-30 の Page Bundle 移行（[[#writing-workflow][Writing Workflow]] 参照）
により、ox-hugo 経由で生成した記事では、記事本文中の ~[[file:foo.jpg]]~
のような画像参照も Page Resource として正しく認識されるようになった。
ただし ~org2hugo.py~ 側は「~[[file:xxx.jpg]]~ を pandoc が単純に
~![](xxx.jpg)~ に変換するだけで、Page Bundle 化に伴うパスの付け替えまでは
未検証」という既知の懸念がスクリプト自身のコメントに残っている
（[[#deploy][Deploy]] や ~org2hugo.py~ 冒頭のコメント参照）。いずれの
経路で生成した場合でも、~hugo~ のビルドは画像参照切れをエラーとして
検出しないため、*ビルドが成功したからといって画像が正しく表示されている
とは限らない*。記事を大きく触った後は、実際に公開されたページを
目視で確認すること。

* License

Content © YAMASHITA, Takao. All rights reserved.
Theme: [[https://github.com/nunocoracao/blowfish][Blowfish]] — MIT License.
